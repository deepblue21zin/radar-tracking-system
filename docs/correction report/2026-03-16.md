# 2026-03-16 Correction Report

## 작성 규칙
- 같은 날짜에 다시 수정이 들어오면 기존 항목을 삭제하지 않고 `~~취소선~~`으로 남긴 뒤, 바로 아래에 최신 내용을 추가한다.
- 이 문서는 "어디를 왜 수정했는지"를 추적하는 작업 로그다.

## Update 1
시각: 2026-03-16 16:55

### 1. `Doxyfile` 추가
- 수정 내용:
  - 프로젝트 루트에 `Doxyfile`을 추가했다.
  - `README.md`, `docs/architecture.md`, `docs/doxygen_overview.md`, `src/`를 입력으로 설정했다.
  - `src/filterpy-master`와 Doxygen 생성물, 실험/evidence 폴더는 제외했다.
  - Graphviz `dot`를 바로 사용할 수 있도록 `DOT_PATH`를 `C:/Program Files/Graphviz/bin`으로 고정했다.
- 수정 이유:
  - `servo_control_baqu`와 같은 방식으로 로컬 HTML 문서와 그래프를 생성할 수 있는 기준 설정이 필요했다.

### 2. `docs/doxygen_overview.md` 추가
- 수정 내용:
  - 레이더 입력부터 parser, filter, cluster, tracking, visualization, STM32 전달까지 이어지는 상위 pipeline map을 Doxygen 메인 페이지용으로 정리했다.
  - Doxygen 안에서 어디부터 읽으면 되는지 reading order와 view별 용도를 함께 적었다.
- 수정 이유:
  - 기본 Doxygen 출력만으로는 파일/함수 단위로 흩어져 보여서 전체 구조를 한눈에 잡기 어려웠다.

### 3. `.gitignore`에 `docs/doxygen/` 추가
- 수정 내용:
  - Doxygen 생성 산출물 전체를 Git에서 무시하도록 `.gitignore`에 `docs/doxygen/`를 추가했다.
- 수정 이유:
  - HTML/SVG/JS 파일이 대량으로 생성되므로 작업트리를 불필요하게 어지럽히지 않기 위해서다.

### 4. Doxygen 실행 결과 확인
- 수정 내용:
  - `doxygen Doxyfile`를 실행해 `docs/doxygen/html/index.html`이 실제로 생성되는 것까지 확인했다.
- 수정 이유:
  - 설정 파일만 넣고 끝내지 않고, 실제로 문서 생성이 성공하는지 검증할 필요가 있었다.

## Update 2
시각: 2026-03-16 17:20

### 1. `Doxyfile` 재조정
- 수정 내용:
  - `md=Markdown` extension mapping을 제거하고 `py=Python`만 유지했다.
  - Graphviz 출력 포맷을 `svg`에서 `png`로 변경했다.
  - Windows 환경에서의 그래프 후처리 충돌을 줄이기 위해 `INTERACTIVE_SVG`를 끄는 방향으로 보정했다.
- 수정 이유:
  - 첫 실행에서 Markdown 매핑 경고와 일부 SVG/HTML rename 경고가 발생해, 재생성 안정성을 높일 필요가 있었다.

### 2. 문서 생성 재검증
- 수정 내용:
  - 조정 후 다시 `doxygen Doxyfile`를 실행해 `docs/doxygen/html/index.html`이 계속 생성되는지 확인했다.
  - `tracker_utils.c/h`의 기존 Doxygen 주석 중복으로 인한 warning은 남았지만, 메인 문서와 그래프 산출물은 생성되는 것을 확인했다.
- 수정 이유:
  - 사용자가 반복해서 문서를 갱신할 때 전체 파이프라인 문서가 깨지지 않는지 확인해야 했기 때문이다.

## Update 3
시각: 2026-03-16 17:30

### 1. `docs/elevation/runtime_issue_fix_plan.md` 추가
- 수정 내용:
  - 현재 운영 중인 파이프라인(`TLV Parsing -> Filter -> DBSCAN -> Kalman Tracking -> Visualization`) 기준으로 실제 문제를 한 문서에 통합 정리했다.
  - DBSCAN 의존성 설치 방법, filter 실효화 방향, parser 안정화 방향, Kalman tracker 개선 방향을 각각 상세히 적었다.
  - 최근 runtime log에서 확인한 수치(`parse_failures`, `resync_events`, `dropped_frames_estimate`, `raw==filtered`, `tracks>clusters`)를 근거로 왜 이 문제를 먼저 봐야 하는지도 정리했다.
- 수정 이유:
  - 사용자가 현재 연결된 파이프라인 기준으로 무엇이 진짜 문제인지, 그리고 어떤 순서로 손봐야 하는지를 한 번에 볼 수 있는 실행 계획 문서가 필요했다.

### 2. `docs/elevation/README.md` 링크 갱신
- 수정 내용:
  - elevation 보드 문서 목록에 `runtime_issue_fix_plan.md` 링크를 추가했다.
- 수정 이유:
  - 새 문서가 다른 파트 문서와 분리되지 않고, 진척 보드의 일부로 바로 접근 가능해야 했기 때문이다.
