# 2026-03-18 Correction Report

## 작성 규칙
- 같은 날짜에 다시 수정이 들어오면 기존 항목을 삭제하지 않고 `~~취소선~~`으로 남긴 뒤, 바로 아래에 최신 내용을 추가한다.
- 이 문서는 "어디를 왜 수정했는지"를 추적하는 작업 로그다.

## Update 1
시각: 2026-03-18 00:20

### 1. `docs/elevation/runtime_issue_fix_plan.md` 상태 정리
- 수정 내용:
  - DBSCAN 환경 문제와 filter 로그/keepout/static clutter 1차 구현처럼 이미 반영된 항목은 취소선으로 표시했다.
  - filter 파트에 "현재 코드 상태"와 "다음 수정 포인트"를 추가했다.
  - 기존의 초기 계획 순서를, 현재 구현 이후에 실제로 다시 봐야 할 튜닝 순서로 갱신했다.
- 수정 이유:
  - 계획 문서에 이미 해결된 항목과 아직 남은 항목이 섞여 있어, 다음 작업 우선순위를 읽기 어렵던 상태를 정리할 필요가 있었다.

## Update 2
시각: 2026-03-18 01:55

### 1. near-front keepout 기본값 축소
- 수정 내용:
  - `src/parser/tlv_parse_runner.py`, `src/visualization/live_rail_viewer.py`의 `--near-front-distance` 기본값을 `1.0m`에서 `0.3m`로 줄였다.
- 수정 이유:
  - 2026-03-18 왕복 측정 로그에서 near-front keepout이 대부분의 제거를 담당했고, 사람이 레이더 가까이 왔을 때 타깃도 같이 잘렸을 가능성이 컸다.

### 2. `runtime_issue_fix_plan.md`에 최신 왕복 run 해석 반영
- 수정 내용:
  - `frames_20260318_014146.csv` 기준 filter/continuity 상태를 요약에 추가했다.
  - filter 파트에 "사람 이동이 연속적으로 보이지 않는 이유"와 다음 튜닝 순서를 반영했다.
- 수정 이유:
  - 사용자의 현재 관심사는 "사람이 실제로 연속적으로 보이느냐"이므로, 단순히 filter가 점을 줄였는지보다 continuity 관점이 문서에 드러나야 했다.

## Update 3
시각: 2026-03-18 02:05

### 1. `max_range` 기본값을 `3.0m`로 조정
- 수정 내용:
  - `src/parser/tlv_parse_runner.py`, `src/visualization/live_rail_viewer.py`의 `--max-range` 기본값을 `None`에서 `3.0m`로 변경했다.
  - `README.md`, `docs/elevation/runtime_issue_fix_plan.md`에도 현재 기본 preset으로 반영했다.
- 수정 이유:
  - 실내 왕복 측정 로그에서 surviving point가 `5.5m` 근처에 자주 남아 far clutter가 사람 위치 해석을 방해하고 있었기 때문이다.

## Update 4
시각: 2026-03-18 02:35

### 1. 기본 `z gate`를 레이더 높이 실측값 기준으로 조정
- 수정 내용:
  - `src/parser/tlv_parse_runner.py`, `src/visualization/live_rail_viewer.py`의 `--filter-z-min/max` 기본값을 `None`에서 `-0.6m ~ 1.0m`로 변경했다.
  - `README.md`, `docs/elevation/runtime_issue_fix_plan.md`에 현재 기본 vertical ROI를 반영했다.
- 수정 이유:
  - 레이더 설치 높이를 바닥 기준 약 `0.83m`로 실측한 뒤, `z=0`이 바닥이 아니라 레이더 높이라는 점이 명확해졌다.
  - 이에 따라 바닥 반사(`z≈-0.83`)와 천장 쪽 반사를 줄이면서 사람 몸통 높이대를 우선 남기는 기본 `z gate`가 필요했다.

## Update 5
시각: 2026-03-18 02:50

### 1. filter 튜닝용 실험 표 추가
- 수정 내용:
  - `docs/elevation/runtime_issue_fix_plan.md`에 파라미터 튜닝 디버깅 표를 추가했다.
  - `z`, `range`, `x/y ROI`, `SNR`, `static clutter`를 어떤 순서로 바꾸고 무엇을 비교해야 하는지 run 단위로 정리했다.
  - `avg_filter_ratio`, `zero_track_frames`, `longest_zero_track_streak`, `avg_fps`, parser 상태를 함께 적는 기록용 표도 추가했다.
- 수정 이유:
  - 지금 단계에서는 파라미터를 감으로 바꾸기보다, 같은 장면에서 한 변수씩 바꾸며 continuity와 clutter 감소를 비교하는 방식이 훨씬 유의미하다.

## Update 6
시각: 2026-03-18 03:05

### 1. Doxygen 가독성 개선
- 수정 내용:
  - `Doxyfile`에 `docs/elevation`, `docs/runtime_logging_roi_plan.md`를 입력으로 추가했다.
  - Graphviz 출력 형식을 `svg`로 바꾸고, heading TOC 깊이를 늘려서 HTML에서 구조를 더 빨리 찾을 수 있게 했다.
  - `docs/doxygen_overview.md`를 "현재 실제 파이프라인", "안 쓰는 경로", "어디부터 읽을지", "증상별 디버깅 파일" 중심으로 다시 작성했다.
- 수정 이유:
  - 기존 Doxygen 첫 화면은 그래프는 있었지만, 지금 실제로 어디가 핵심 경로인지와 어디부터 읽어야 하는지가 한눈에 들어오지 않았다.

### 2. runtime issue 문서 표 폭 축소
- 수정 내용:
  - `docs/elevation/runtime_issue_fix_plan.md`의 넓은 표를 더 작은 표 여러 개로 나눴다.
  - 실험 순서, 해석 메모, 기록용 표를 분리해 브라우저에서 덜 깨지게 정리했다.
- 수정 이유:
  - 기존 표는 열 수가 많아 Markdown preview와 Doxygen HTML 양쪽에서 읽기 어려웠다.

## Update 7
시각: 2026-03-18 14:20

### 1. runtime runner 구조 분리 반영
- 수정 내용:
  - `src/parser/tlv_parse_runner.py`를 호환 래퍼로 정리했다.
  - 실제 런타임 구현을 `src/parser/runtime_pipeline.py`로 분리했다.
  - 공용 파라미터 로더 `src/runtime_params.py`와 `config/runtime_params.json`을 추가했다.
- 수정 이유:
  - dev 병합 과정에서 runner 충돌이 커졌고, 반복 튜닝 값이 runner/viewer 양쪽에 흩어져 있어 유지보수가 어려웠다.

### 2. README 및 핵심 문서 동기화
- 수정 내용:
  - `README.md`, `docs/architecture.md`, `docs/doxygen_overview.md`, `docs/capstone_pipeline_spec.md`, `docs/TLV.md`, `docs/study/study_runtime_pipeline_and_control.md`를 새 구조에 맞게 갱신했다.
  - `docs/study/study_TLV.md`의 핵심 코드 링크를 `runtime_pipeline.py` 기준으로 옮겼다.
  - `docs/elevation/runtime_issue_fix_plan.md`에서 `send_config()` 이후 input buffer reset 미반영 항목을 반영 완료 상태로 정리했다.
- 수정 이유:
  - 실행 명령은 여전히 `tlv_parse_runner.py`를 쓰지만, 실제 구현과 파라미터 관리 위치가 바뀌어서 문서가 그대로면 팀원이 잘못된 파일을 계속 보게 된다.

## Update 8
시각: 2026-03-18 22:25

### 1. viewer 움직임 인지 보강 및 축 보정 옵션 추가
- 수정 내용:
  - `src/visualization/live_rail_viewer.py`에 `motion cloud`, `track trail`, `velocity arrow`, `top-down motion` 패널을 추가했다.
  - viewer 오버레이에 `removed_range/roi/keepout/static`, `parse_failures/resync_events/dropped_frames_estimate`, `sensor_yaw_deg`를 표시하도록 했다.
  - `src/parser/runtime_pipeline.py`, `src/parser/tlv_parse_runner.py`, `src/visualization/live_rail_viewer.py`에 `--sensor-yaw-deg`를 추가했다.
  - `src/parser/runtime_pipeline.py`의 `MMWaveSerialReader` 기본 버퍼를 `32KB`에서 `128KB`로 늘렸다.
  - `src/runtime_params.py`, `config/runtime_params.json`에 viewer motion/persistence 파라미터와 `sensor_yaw_deg` 기본값을 추가했다.
- 수정 이유:
  - 사용자가 레이더를 바라보며 앞뒤로 움직인 영상에서, 현재 viewer는 이동 방향이 직관적으로 보이지 않았고 top-down 축도 설치 방향과 어긋나 보였다.
  - 같은 영상/로그에서 `filtered=4~6`, `clusters=0`, `tracks=0` 구간이 반복되어, 단순 tracker 문제가 아니라 `max_range`, keepout, parser health를 함께 봐야 한다는 점이 드러났다.

### 2. `frames_20260318_215954` 및 영상 근거를 문서화
- 수정 내용:
  - `docs/performance_log.md`에 `frames_20260318_024048`, `frames_20260318_215954` run 요약을 추가했다.
  - `docs/elevation/runtime_issue_fix_plan.md`에 2026-03-18 baseline/full_frame run과 화면 녹화 기반 관측을 반영했다.
  - `docs/study/study_runtime_pipeline_and_control.md`에 viewer의 motion 가시화와 `sensor_yaw_deg` 역할을 설명하는 항목을 추가했다.
- 수정 이유:
  - 이번 변경은 단순 코드 수정이 아니라, 로그와 영상으로 확인된 "왜 사람이 끊기고 축이 틀어져 보이는가"에 대한 진단이었다.
  - 앞으로도 성능이 좋아지거나 나빠지면, 수정 내용과 근거를 같은 날 바로 문서에 남기는 흐름이 필요했다.

## Update 9
시각: 2026-03-18 23:20

### 1. viewer를 runtime 공용 처리 경로 기준으로 정렬
- 수정 내용:
  - `src/parser/runtime_pipeline.py`에 `build_runtime_processing_context()`와 `process_runtime_frame()` 공용 함수를 추가했다.
  - `src/visualization/live_rail_viewer.py`가 자체적으로 raw/filter/cluster/track를 다시 계산하지 않고, 위 공용 함수를 호출하도록 바꿨다.
  - `src/parser/tlv_parse_runner.py`에도 새 공용 함수를 재수출해 기존 import 호환을 유지했다.
- 수정 이유:
  - viewer가 runtime과 따로 처리 순서를 가지면, filter/DBSCAN/tracker를 수정할 때 "runner 결과"와 "viewer 결과"가 어긋날 수 있었다.
  - 사용자가 계속 시각화 기준으로 튜닝하고 있으므로, viewer가 runtime과 같은 함수에서 결과를 받아야 디버깅 근거가 흔들리지 않는다.

### 2. `frames_20260318_225405` 재측정 run 분석 추가
- 수정 내용:
  - `docs/performance_log.md`에 `20260318_225405` run을 추가했다.
  - `docs/elevation/runtime_issue_fix_plan.md`에 `225405`의 zero-track streak, range/ROI 제거량, parser health를 반영했다.
  - `README.md`에 viewer가 `runtime_pipeline.py`의 공용 frame 처리 함수를 사용한다는 설명을 추가했다.
- 수정 이유:
  - 이번 run은 `215954`보다 `resync=62`, `dropped=10`으로 parser health가 좋아졌지만, `zero_track_frames=139/590`으로 continuity는 여전히 약했다.
  - 특히 `frame 68-94`와 `frame 472-507`에서 `removed_range`와 `removed_roi`가 크게 누적되어, 앞뒤 이동 미검출의 주원인이 range/ROI 쪽임을 문서에 바로 남길 필요가 있었다.

## Update 10
시각: 2026-03-18 23:45

### 1. `frames_20260318_231830` 장시간 왕복 run 해석 추가
- 수정 내용:
  - `docs/performance_log.md`에 `20260318_231830` run을 추가했다.
  - 이번 run에서 `zero_track_frames=552/1299`, 최장 zero-track streak `frame 833-906`과 그 구간의 `removed_range=760`, `removed_roi=211`를 기록했다.
  - preview track 좌표에서 `y=1.74 -> 0.60 -> 1.31`, `y=1.30 -> 0.32 -> 1.12`처럼 앞뒤 왕복이 읽히는 점과, 동시에 `x` 변화폭도 커서 축 정렬이 아직 완전하지 않다는 점을 함께 남겼다.
- 수정 이유:
  - 이 run은 "앞뒤 왕복이 로그에 실제로 보이느냐"를 확인하기 좋은 장시간 샘플이었다.
  - 결론적으로 왕복 자체는 보이지만 continuity는 나빠졌고, 원인이 parser보다는 여전히 `range + ROI gate` 쪽이라는 점을 같은 날 바로 문서에 남길 필요가 있었다.

## Update 11
시각: 2026-03-18 23:58

### 1. CSV 프레임 로그의 overview PNG 자동 생성 추가
- 수정 내용:
  - `src/visualization/runtime_log_overview.py`를 추가해 frame CSV를 읽고 요약 PNG를 렌더링하는 공용 함수를 만들었다.
  - `src/parser/runtime_pipeline.py` 종료 루틴에서 frame CSV 저장 후 자동으로 `frames_YYYYMMDD_HHMMSS_overview.png`를 생성하도록 연결했다.
  - `README.md`의 측정/기록 섹션에도 자동 PNG 생성 사실을 반영했다.
- 수정 이유:
  - 사용자가 매번 CSV를 보고 별도로 시각화 이미지를 만들기보다, run이 끝날 때 바로 비교용 PNG가 같이 남는 편이 실험 관리에 훨씬 낫다.
  - 같은 형식의 overview PNG가 자동 누적되면 run 간 비교와 문서화도 쉬워진다.
