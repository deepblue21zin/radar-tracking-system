# 2026-03-17 Correction Report

## 작성 규칙
- 같은 날짜에 다시 수정이 들어오면 기존 항목을 삭제하지 않고 `~~취소선~~`으로 남긴 뒤, 바로 아래에 최신 내용을 추가한다.
- 이 문서는 "어디를 왜 수정했는지"를 추적하는 작업 로그다.

## Update 1
시각: 2026-03-17 03:40

### 1. `src/parser/tlv_parse_runner.py` 로그 형식 개선
- 수정 내용:
  - 프레임 CSV에 `frame_summary` 컬럼을 추가했다.
  - 각 프레임마다 `frame=... gap=... packet=... num_obj=...` 형식의 한 줄 요약을 CSV에도 같이 저장하도록 바꿨다.
  - 실행 중 콘솔에 찍히는 프레임 로그와 성능 로그를 `frames_YYYYMMDD_HHMMSS.log` 텍스트 파일에도 함께 남기도록 추가했다.
- 수정 이유:
  - 기존 `frames_*.csv`는 컬럼형 분석에는 좋았지만, IDE나 텍스트 뷰어에서 빠르게 읽기엔 숫자 나열처럼 보여 사용성이 떨어졌다.

### 2. `docs/runtime_logging_roi_plan.md` 문서 갱신
- 수정 내용:
  - `frames_*.log` 텍스트 로그가 함께 저장된다는 점을 문서에 반영했다.
  - `frame_summary` 컬럼의 의미와 예시 한 줄을 추가했다.
- 수정 이유:
  - 로그 형식이 바뀌었으므로, 실행 결과를 어디서 어떤 형식으로 봐야 하는지 문서도 같이 맞춰둘 필요가 있었다.

## Update 2
시각: 2026-03-17 04:10

### 1. `src/filter/noise_filter.py` 필터 확장
- 수정 내용:
  - 기존 `snr/noise/range/z` 필터를 `x/y/z inclusive ROI`, `near-front keepout`, `right-rail keepout`, `low-velocity static clutter reject`까지 확장했다.
  - 프레임별 `filter_ratio`, `snr/range` 분포, 제거 사유별 카운트, 샘플 point preview를 담는 `FilterStats`를 추가했다.
- 수정 이유:
  - 이전 로그에서는 `raw_points == filtered_points`가 계속 나와 실제 filter가 scene에서 거의 작동하지 않았고, 어느 규칙이 효과가 있는지 숫자로 판단할 정보도 부족했다.

### 2. `src/parser/tlv_parse_runner.py` 런타임 로그 강화
- 수정 내용:
  - runner가 `FilterStats`를 받아 프레임 CSV와 텍스트 로그에 `filter_ratio`, `snr/range` 통계, keepout/static clutter 제거량, sample preview를 남기도록 바꿨다.
  - near-front 1m keepout, right-rail keepout, static clutter reject를 기본 활성화하고 각 파라미터를 CLI 인자로 조정 가능하게 추가했다.
- 수정 이유:
  - 사용자가 매 프레임에서 filter가 실제로 점을 줄였는지와, front clutter / rail clutter가 얼마나 잘 깎였는지를 바로 확인할 수 있어야 했다.

### 3. `src/visualization/live_rail_viewer.py`와 문서 동기화
- 수정 내용:
  - live viewer도 runner와 같은 keepout/static clutter 규칙을 사용하도록 맞췄다.
  - `docs/runtime_logging_roi_plan.md`, `docs/elevation/filter.md`에 새 로그 컬럼과 filter preset 상태를 반영했다.
- 수정 이유:
  - 콘솔/CSV에서 본 filter 결과와 3D 시각화 결과가 서로 다른 규칙을 쓰면 디버깅이 오히려 더 어려워지기 때문이다.
