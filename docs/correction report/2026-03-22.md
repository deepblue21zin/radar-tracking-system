# 2026-03-22

## 15:20

- 수정 파일
  - `docs/system_requirements/README.md`
  - `docs/system_requirements/REQ-RTOS-TASK-ARCHITECTURE.md`
  - `docs/system_requirements/REQ-PERFORMANCE-BENCHMARK.md`
  - `docs/system_requirements/REQ-INTERFACE-CONTRACT.md`
  - `docs/system_requirements/REQ-SAFETY-POLICY.md`
  - `docs/system_requirements/index.html`
  - `README.md`

- 변경 내용
  - 레이더 SW를 자동차 SW 포트폴리오 관점에서 설명하기 위한 시스템 요구사항 패키지를 새로 만들었다.
  - RTOS task 구조, 성능 benchmark 요구사항, 인터페이스 계약, safety policy를 각각 REQ 문서로 분리했다.
  - 로컬 브라우저에서 바로 볼 수 있도록 `docs/system_requirements/index.html` 포털을 추가했다.
  - `README.md` 관련 문서 섹션에 새 요구사항 포털 링크를 추가했다.

- 변경 이유
  - 기존 문서는 알고리즘/실험 중심 설명이 강했고, RTOS/입출력 계약/fail-safe 관점의 명세가 분산되어 있었다.
  - 현대모비스 등 자동차 SW 직무 포트폴리오에서 요구하는 "요구사항 기반 설명"을 강화하기 위해 별도 패키지로 정리했다.

- 영향 범위
  - 문서 구조
  - 포트폴리오/면접 자료
  - 로컬 HTML 문서 탐색 경로
## 16:05

- 수정 파일
  - `docs/system_requirements/index.html`
  - `docs/correction report/2026-03-22.md`

- 변경 내용
  - 시스템 요구사항 포털의 상단 버튼이 raw markdown 파일로 이동하던 흐름을 제거하고, 같은 페이지 안의 상세 REQ 섹션으로 이동하도록 구조를 바꿨다.
  - 상세 REQ 내용도 첫 화면과 동일한 카드/테이블/메타 패널 스타일로 읽히도록 HTML을 재구성했다.
  - 각 상세 섹션에는 `원문 md 보기` 링크를 별도로 두어, styled view와 source view를 분리했다.

- 변경 이유
  - 첫 화면은 styled HTML인데 상세 버튼 클릭 후 raw markdown이 열리면서 가독성과 시각적 일관성이 크게 떨어졌다.
  - 요구사항 포털을 포트폴리오 문서로 활용하려면 overview와 detail이 같은 문서 경험을 유지해야 했다.

- 영향 범위
  - `docs/system_requirements/index.html` 사용자 경험
  - REQ 상세 문서 탐색 방식
## 16:35

- 수정 파일
  - `docs/system_requirements/req_theme.css`
  - `docs/system_requirements/package_overview.html`
  - `docs/system_requirements/req_rtos_task_architecture.html`
  - `docs/system_requirements/req_performance_benchmark.html`
  - `docs/system_requirements/req_interface_contract.html`
  - `docs/system_requirements/req_safety_policy.html`
  - `docs/system_requirements/index.html`
  - `docs/correction report/2026-03-22.md`

- 변경 내용
  - `패키지 개요 보기`를 raw markdown이 아니라 styled HTML 문서 `package_overview.html`로 연결되도록 수정했다.
  - 각 REQ 상세도 raw markdown 대신 개별 styled HTML 페이지로 추가했다.
  - 포털의 `원문 보기` 계열 링크는 동일한 디자인의 HTML 상세 페이지로 연결되도록 바꾸고, 각 상세 페이지 안에서만 `md 원문 보기`를 보조 링크로 두었다.
  - `req_theme.css`를 추가해 포털/상세/개요 페이지가 같은 시각 톤을 공유하도록 정리했다.

- 변경 이유
  - 메인 포털은 styled HTML인데 개요/상세 링크가 raw markdown으로 빠져 문서 경험이 다시 깨졌다.
  - overview/detail/source의 계층을 분리해, 기본 탐색은 항상 styled HTML로 이어지게 만들 필요가 있었다.

- 영향 범위
  - `docs/system_requirements/` 전체 문서 탐색 경험
  - 포털과 상세 REQ 문서의 시각 일관성
## 16:45

- 수정 파일
  - `docs/system_requirements/index.html`
  - `docs/correction report/2026-03-22.md`

- 변경 내용
  - 포털에서 상세 HTML 페이지로 연결되는 버튼 라벨을 `원문 md`에서 `상세 페이지`로 바꿨다.
  - 메인 포털에서는 styled HTML 흐름만 보이게 하고, 실제 md 원문은 상세 페이지 내부 보조 링크로만 남기도록 정리했다.

- 변경 이유
  - 링크 대상은 이미 styled HTML로 바뀌었는데 버튼 라벨이 `원문 md`로 남아 있어 사용자 기대와 실제 동작이 어긋났다.

- 영향 범위
  - `docs/system_requirements/index.html` 탐색 문구

## 17:00

- 수정 파일
  - `.gitignore`
  - `docs/correction report/2026-03-22.md`

- 변경 내용
  - 로컬 영상/임시 분석 산출물이 Git 상태에 계속 나타나지 않도록 `tmp/`를 `.gitignore`에 추가했다.
  - stray 파일과 임시 폴더 정리 작업을 위한 기록을 남겼다.

- 변경 이유
  - `tmp/video_inspect_*`는 실험 중간 산출물이라 버전 관리 대상이 아니고, Git 상태를 불필요하게 오염시켰다.
  - 명령 오타로 생성된 stray 파일도 함께 정리해 작업트리를 깔끔하게 유지할 필요가 있었다.

- 영향 범위
  - 로컬 임시 산출물 Git 추적 제외
  - 작업트리 가독성 개선

## Update 7
시각: 2026-03-22 16:55

### 1. 교수님 발표용 수정 이유 PPT 생성
- 수정 내용:
  - `docs/presentation/radar_modification_story_outline.md`를 기준으로 발표용 PowerPoint 자동 생성 스크립트 `docs/presentation/generate_radar_modification_story.ps1`를 추가했다.
  - `docs/presentation/radar_modification_story_2026-03-22.pptx`를 생성했다.
  - 발표 시 바로 읽을 수 있도록 `docs/presentation/radar_modification_story_notes.md`를 추가했다.
- 수정 이유:
  - 사용자가 “라이브러리 도입 이후 왜 계속 수정이 필요했는지”를 교수님께 발표해야 해서, 변경 이유를 시간 순서와 시스템 엔지니어링 관점으로 바로 설명할 수 있는 자료가 필요했다.

## Update 8
시각: 2026-03-22 17:35

### 1. 교수님 발표용 PPT 레이아웃 재설계
- 수정 내용:
  - `docs/presentation/generate_radar_modification_story.ps1`를 카드형 레이아웃과 짧은 문장 중심 구조로 다시 작성했다.
  - 한 슬라이드당 정보량을 줄이고, `Malgun Gothic` 기준으로 텍스트 넘침 가능성이 낮은 배치로 조정했다.
  - 미리보기 검증을 위해 `docs/presentation/radar_modification_story_preview_final/` PNG 출력도 함께 생성하도록 했다.
  - 최종 발표 파일 `docs/presentation/radar_modification_story_2026-03-22_final.pptx`를 생성했다.
- 수정 이유:
  - 기존 버전은 글자량이 많고 일부 줄바꿈/배너 영역에서 깨져 보일 수 있어, 실제 발표 시 가독성이 떨어졌다.
  - 발표용 자료는 내용보다도 “한 화면에서 바로 읽히는 구조”가 중요하므로, 밀도를 낮추고 시각적 안정성을 우선해 다시 설계했다.

## Update 9
시각: 2026-03-22 18:20

### 1. runtime frame processing 책임 분리
- 수정 내용:
  - `src/runtime/models.py`를 추가해 `ParsedFrame`, `ReaderStats`, `RuntimeProcessingContext`, `RuntimeFrameProcessingResult`, `RuntimeFrameHookPayload`를 옮겼다.
  - `src/runtime/processing.py`를 추가해 `transform_points_to_world()`, `build_keepout_boxes()`, `build_static_clutter_boxes()`, `build_runtime_processing_context()`, `process_runtime_frame()`를 옮겼다.
  - `src/parser/runtime_pipeline.py`는 serial reader, logger, realtime orchestration 중심으로 정리하고, 위 processing/model 모듈을 import해 쓰도록 바꿨다.
  - `src/visualization/live_rail_viewer.py`는 `RuntimeFrameHookPayload`를 `runtime_pipeline.py`가 아니라 `src/runtime/models.py`에서 직접 가져오도록 수정했다.
- 수정 이유:
  - 기존 `runtime_pipeline.py` 안에 world transform, filter/cluster/track orchestration, runtime hook payload까지 함께 들어 있어 “실행 루프”와 “프레임 처리 로직”의 경계가 흐려졌다.
  - 현재 구조에서는 filter/clustering/tracking 모듈이 분리돼 있어도, 실제 읽는 사람 입장에서는 런타임 파일이 다시 모든 책임을 먹어버린 것처럼 보였기 때문에 역할을 재분리할 필요가 있었다.
- 영향 범위:
  - 실행 동작 자체를 바꾸기보다 모듈 책임을 분리한 리팩토링이다.
  - `python src/parser/tlv_parse_runner.py --help`, `python src/visualization/live_rail_viewer.py --help` 기준 import smoke test를 통과했다.
